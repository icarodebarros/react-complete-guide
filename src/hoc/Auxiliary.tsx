import React, { PropsWithChildren } from 'react';

// eslint-disable-next-line @typescript-eslint/no-explicit-any
const aux: React.FC = (props: PropsWithChildren<any>) => props.children;

export default aux;